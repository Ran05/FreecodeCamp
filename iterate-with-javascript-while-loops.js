/**
Basic JavaScript: Iterate with JavaScript
 While Loops
You can run the same code multiple times
 by using a loop.

The first type of loop we will learn 
is called a while loop because it runs 
"while" a specified condition is true 
and stops once that condition is no longer true.
 */

// Setup
var myArray = [];

  var i = 5;
    while(i >= 0) {
      myArray.push(i);
     // myArray.reverse(i);
      i--;
    }
     

// Only change code below this line
